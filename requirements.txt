# Requires Python >= 3.9 (3.10 recommended)
# Core runtime for the composer + DSL
numpy>=1.24
regex>=2024.5.15

# Optional helpers (auto-detected by capability probes)
orjson>=3.10
datasets>=2.20.0
opencc>=1.1.6

# Optional dedupe / ANN accelerators
simhash>=2.1.2
datasketch>=1.5.9
annoy>=1.17
numba>=0.58
xxhash>=3.4
faiss-cpu>=1.7.4 ; platform_system != "Windows"

# Soft-evidence extraction pipeline (extract_and_bank_zh_phrases.py)
jieba>=0.42.1
sentence-transformers>=2.7.0
scikit-learn>=1.3
torch>=2.1
# Optional: install jieba-fast for faster tokenisation when wheels are available
