# Base runtime dependencies
numpy>=1.24
regex>=2024.5.15
pyyaml>=6.0

# Optional data sources / DSL helpers
datasets>=2.20.0
opencc>=1.1.6
orjson>=3.10

# Optional accelerators (used when available; falls back gracefully if missing)
annoy>=1.17
datasketch>=1.5.9
faiss-cpu>=1.7.4; platform_system != "Windows"
numba>=0.58
scikit-learn>=1.3
simhash>=2.1.2
xxhash>=3.4
